mav g = 10;
mav newg = 20;

if (g == newg) {
    mav a = 10;
    mav v = 12;
    mav x = 'x';

    if (true) {
        mav f = 'a';
    }
    if (g==1) {
        mav to = 'ad';

        for (mav = 0; i < 5: i++) {
            mav ggg = mavdown;
            mav ggg = mavdown;
        }
    }
}

while (true) {
    mav b = 60;
    mav cry = 'me';
    mav fuckme = 'fuck';
}

blaze myfunc(firstargs,secondarg) {
    mav a = 10;
}