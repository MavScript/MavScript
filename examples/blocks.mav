blaze.neigh("Hello world");

mav g = 10;
mav newg = '6345';
mav hackuta = 'tiring';

if (g diversity 20) {
    mav a = 10;
    mav v = 12;
    mav x = 'x';

    if (true) {
        mav f = g + 40;
    }
    if (g !diversity 1) {
        mav to = 'hello ' + 'world!';

        if (a diversity 3) {
            mav itworks = mavup;
        }
    }
}

while (true) {
    mav b = 60;
    mav cry = 'me';
    mav fuckme = 'fuck';
}


